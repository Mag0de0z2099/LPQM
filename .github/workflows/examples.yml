name: examples CI
on:
  push: { branches: [ "main" ], paths: [ "examples/**" ] }
  pull_request: { branches: [ "main" ], paths: [ "examples/**" ] }
permissions: { contents: read }
concurrency: { group: examples-${{ github.ref }}, cancel-in-progress: true }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          if [ ! -d "examples" ]; then echo "Sin carpeta examples/"; exit 0; fi
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f examples/requirements.txt ]; then pip install -r examples/requirements.txt; fi
          pip install pytest
      - run: |
          cd examples
          if [ -d tests ] && ls tests/*.py >/dev/null 2>&1; then pytest -q
          else python - <<'PY'
print("examples: smoke check OK")
PY
          fi
