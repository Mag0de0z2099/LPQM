name: sim ok

on:
  push:
    branches: [ "main" ]
    paths:
      - 'sim/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'sim/**'

jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Guard clause
        run: |
          if [ ! -d "sim" ]; then
            echo "No existe sim/ en este commit; saliendo en éxito."
            exit 0
          fi
      - run: echo "sim OK"
