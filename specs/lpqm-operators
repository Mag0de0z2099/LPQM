# Operadores base: Transición Ondas ↔ Partículas

## →P(probabilidad, ruido, entorno)
- Intenta colapsar una **onda** a **partícula**.
- Condición típica: `RANDOM() < prob + entorno.impacto - ruido.amortiguación`

## →W(probabilidad, ruido, entorno)
- Intenta dispersar una **partícula** a **onda**.
- Condición típica: `RANDOM() < prob + ruido.estímulo - entorno.restricción`

### Parámetros orientativos
- `prob` ∈ [0,1]
- `ruido`: `{ amortiguación, estímulo }`
- `entorno`: `{ impacto, restricción }`

### Eventos
- `registrar_evento(tipo, t, estado, params)`
